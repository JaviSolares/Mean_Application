apiVersion: v1
kind: Service
metadata:
  name: back-service
  labels:
    app: appback
    role: back-client
    tier: backend
spec:
  ports:
  - port: 5000
    targetPort: 5000
  selector:
    app: appback
    role: back-client
    tier: backend